# Query designed to pass all Apollo Armor limits
# Simple query that should not trigger any violations
query ValidQueryTest {
  user(id: "test") {
    id
    name
    wallets {
      id
      community {
        id
        name
      }
    }
    opportunities {
      id
      title
      place {
        id
        name
      }
    }
  }
}
